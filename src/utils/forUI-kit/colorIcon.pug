mixin colorIcon(color, name, opacity)
  div.color-icon__container
    span(class="color-icon__color" style=`background-color: ${color}; opacity: ${opacity}`)
    span
      p(class="color-icon__title") #{name}
      br
      p(class="color-icon__subtitle") #{color} 
