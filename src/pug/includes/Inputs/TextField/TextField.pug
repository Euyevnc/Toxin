mixin TextField(label, placeholder, describe, active)
    - if(!active){active=''}
    div(class=`TextField`)
        
        label(style="font-weight:bold") #{label}
            div
                input(class="TextFieldValue"+`${active}` placeholder=`${placeholder}`) 
        p(class="TextFieldDescribe") #{describe}
        