mixin Card(text, links)    
    div.card
        span.card__logo 
            include ../../../static/Images/logo.img_color.svg
            include ../../../static/Images/TOXIN.svg
        span.card__subscribe
            p #{text}
        span.card__social
            +Social(links)