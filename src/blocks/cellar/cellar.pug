include ../social/social.pug
mixin Cellar(text, links)
    div.cellar 
        span.cellar__text-container
            p.cellar__text #{text}
        span.cellar__social-container
            +Social(links)