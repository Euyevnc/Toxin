mixin textfield({ label='', placeholder='enter text ', describe='', type='text'}={})
  div(class=`textfield`)
    label(class="textfield__label")
      if label
        h3.textfield__subscribe  #{label}
      input(class="textfield__value" placeholder=placeholder type=type )
    p(class="textfield__describe") #{describe}
    