include ../textfield/textfield.pug
mixin date-picker({ textfield, initArrive=0, initDeparture=0} = { })
  div(class=`js-date-picker date-picker` data-initArrive=initArrive data-initDeparture = initDeparture)
    +textfield({ ...textfield, arrow: 'down' })